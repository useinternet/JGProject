

Name  
{
     RootSignature
    {
        // DescriptorTable, CBV, SRV, UAV, Constant
        DT(이름, 자료형 )  = {타입, registerNumber, registerSpace, visibility},
        CBV(이름, 자료형) = {registerNumber, registerSpace, visibility},
        SRV(이름,자료형 ) = {registerNumber, registerSpace, visibility},
        UAV(이름, 자료형) = {registerNumber, registerSpace, visibility},
        Constant(이름, 자료형) =  {32bitNum, registerNumber, registerSpace, visibility},
        StaticSampler 
        {   
            이름 = 
            {
                registerNumber,
                sampler 특징
            }
        }
    }
    // 자동(미구현)
    RootSignature : auto
    {
        path = "";
    }
    // 각 그래픽 셰이더 모듈에 바인딩 및 ID 부여 Head ID는 무조건 0 Tail ID는 무조건 -1
    RegisterSM
    {
        {ID, path},
        {ID, path},
        ...
    }

    Head
    {
        // ConnectID, renderTargetIndex...
        Connect(ConnectID, ...),
        Connect(ConnectID, ...)

    }


    Node
    {   
        ID(..)
        {
            Connect(ConnectID, ...),

        }





    }



    Tail
    {
        // 딱히 할게없다.
    }



}