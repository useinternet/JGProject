		// 필요한 변수
		// 평면에 직각으로 수직으로한 법선 벡터
		// ax by cz d = 0;
		// d = -ax-by-cz;
		// 세점( p0, p1, p2) 
		// 1. p0p1(v0), p0p2(v1) 의 벡터를 구하고 외적하여 법선 벡터(n)를 만든다.
		// 2. a = n.x b = n.y c = n.z
		// 3. d = -n.x * v0.x    -n.y * v0.y    -n.z * v0.z;